// const { postProjects } = require('../../database/queries/projects/postProjects.js');

exports.postProjects = (req, res) => {
  // const uId = req.params.user_id;

  res.status(201).send('post projects got');
};
